# RBAC Client

Next.js 14 (App Router) frontend for the RBAC system.

## Setup

Create `client/.env.local`:

```
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```

## Run

```
npm install
npm run dev
```

